# TO READ
This is the open-source code of the SENTINEL project, aiming to help user to protect their private life

# MESSAGE TO THE CREATORS AND COLLABORATORS

The .env files are avaiable on the SENTINEL Manager App 

The .logs files are avaible on the SENTINEL Manager App 



The node_modules aren't public in this repository, you can install it by this commands :
`npm install electron`

`npm install dotenv`

`npm install electron-store`

`npm install systeminformation`

`npm install --save-dev electron-builder`

`npm install chokidar`

`npm install better-sqlite3`